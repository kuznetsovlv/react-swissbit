# Changesets

Run `pnpm changeset` for user-facing changes that should be included in a
release.

Choose the version bump according to the public API impact of the change.

Infrastructure-only changes that do not affect the published API do not require
a changeset.
